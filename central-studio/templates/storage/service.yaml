{{- $conf := mustMergeOverwrite (dict) (deepCopy .Values.global) (deepCopy .Values.storage) -}}
{{- if $conf.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "storage.name" . }}
  labels:
    {{- include "storage.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "storage.selectorLabels" . | nindent 4 }}
{{- end }}